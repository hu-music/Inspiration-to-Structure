# Data

HuggingFace training dataset:

```text
cszhu09876/ios-disco-abc
```

Download to local folders:

```bash
python scripts/download_dataset.py
```

Local output:

```text
data/train_dataset_abc/
data/eval_dataset_abc/
```

Original section-level POP909 annotation package:

```text
data/pop909_section.zip
```

The zip contains the original section-level MIDI and ABC data.
